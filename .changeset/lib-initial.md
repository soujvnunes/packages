---
'@soujvnunes/lib': minor
---

Initial release: stateful modules with optional-peer subpaths — `./mongoose` (serverless Mongoose client: `withDb`, `withDbCallback`, `getDbClient`, `getDB`) and `./typegoose` (`BaseModel`, `BaseTimestampedModel`).
