---
'@soujvnunes/eslint-config': patch
'@soujvnunes/prettier-config': patch
'@soujvnunes/stylelint-config': patch
'@soujvnunes/util': patch
'@soujvnunes/lib': patch
'@soujvnunes/react': patch
---

npm discoverability: add `keywords`, `homepage`, and `bugs` to every package; add the missing `@soujvnunes/stylelint-config` README, and correct the `@soujvnunes/prettier-config` install note (the Tailwind plugin is bundled, not a manual install).
