# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).

Add a changeset for any change that should ship: run `pnpm changeset`, pick the affected packages and a semver bump, then write a one-line summary. On merge to `main`, the release workflow versions + publishes.
