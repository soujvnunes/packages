# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).

Add a changeset for any change that should ship: `pnpm changeset` — pick the affected packages and a semver bump, write a one-line summary. On merge to `main`, the release workflow versions + publishes.
